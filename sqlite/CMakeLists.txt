INCLUDE_DIRECTORIES(
    .
    ./include
)

AUX_SOURCE_DIRECTORY(. SRCS)

MESSAGE("CAMKE SOURCE DIR IS:" ${CMAKE_SOURCE_DIR})

SET(LIBRARY_OUTPUT_DIRECTORY ${CMAKE_SOURCE_DIR}/lib/)

ADD_DEFINITIONS(-DTHREADSAFE=1)
ADD_LIBRARY(sqlite SHARED STATIC ${SRCS})
